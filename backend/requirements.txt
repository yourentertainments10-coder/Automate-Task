Django>=6.0
djangorestframework>=3.16
djangorestframework-simplejwt>=5.5
django-cors-headers>=4.6
python-dotenv>=1.0
requests>=2.32
# Production (Render + Neon Postgres):
psycopg[binary]>=3.2
gunicorn>=23.0
whitenoise>=6.8

# File storage: S3 when AWS_STORAGE_BUCKET_NAME is set, local disk otherwise.
# Needed because a container's own disk is wiped on every deploy.
django-storages[s3]>=1.14
